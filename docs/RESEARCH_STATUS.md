# 持续科研状态

更新时间：2026-09-09。当前阶段：问题与方法必要性审查。当前主线尚未选定。

持续跟进任务已创建并核验：ID `automation`，当前任务内 heartbeat，每 6 小时，状态 ACTIVE。需要使用本地文件时，计算机与桌面应用必须保持运行。暂停或变更时更新既有任务，不创建重复项。

## 已完成

- 建立研究目标、证据标准、计算预算和持续执行流程，见 `RESEARCH_PROGRAM.md`。
- 核查最直接的分层 ICL 与 compositional ABI 先例，见 `LITERATURE_LEDGER.md`。R1 已直接研究组合误差，广义“按组组合 PFN”不能作为新颖性主张。
- 完成已知方差下的高斯随机截距参照，以及组级中心偏差组合后的 KL 分解，见 `GROUPED_EVIDENCE_FOUNDATIONS.md`。结论是解析构造，不是对任何训练模型的实证发现。
- 运行 `../venv/bin/python grouped_gaussian_sanity.py --output results/grouped_foundations_20260909`：16 个完整联合高斯与分组统计量对照通过；5 个组数条件的 KL 数值积分及每格 20,000 次 Monte Carlo 检查通过。
- 后验均值最大绝对差 4.53e-14，方差最大绝对差 3.59e-15。原始核验记录包括 seed 和源文件 SHA-256，位于 `results/grouped_foundations_20260909/verification.json`。
- 仓库已有 9 项测试全部通过，包含小型训练与真实检查点验证；这属于实现回归检查。

## 当前判断

候选 C1：组级摊销误差的条件偏差、跨组相关性与组合后校准。状态为待论证。高斯模型揭示同向偏差项与零均值独立误差项具有不同的累积规律；这个区别仍需与既有 approximate inference 理论对照。

没有启动新的研究模型训练，也没有新 SCNet 作业。本轮 GPU 消耗为 0。此前 pilot 的 12 个作业已经全部完成；如需新作业，重新核查服务器实际状态。

## 下一次执行的具体任务

1. 获取 R1 官方仓库到工作目录下独立的 external 子目录，记录固定 commit，阅读其 damping 与末端 score 实现；不要直接运行下载的脚本或安装步骤。对应检查论文第 3.2 节和附录证明。
2. 针对高斯模型，明确区分采样器离散误差、diffusion 中间分布误差和末端近似似然的系统偏差。判断 d(0)=1 的数值稳定化是否理论上消除最后一项；不能把一类误差的反例当成另一类方法失效。
3. 核查 SBI 中条件校准、score bias 与近似似然误差的已知结果，最多追踪最直接的 3 篇，再写一个候选是否继续的具体判断。不得无限扩充文献清单。
4. 如有未覆盖且有实际意义的机制，写完整主张与实验协议后再实现神经模型。若不存在，则依据最接近论文的实质限制提出一个替代问题，并记录推导，不自动开展广泛模型搜索。

本次工作结束条件是上述证据已经保存、核验完成和后续执行入口明确；它不代表持续科研目标完成。

## 运行与预算台账

| 日期（上海） | 工作 | 状态 | GPU 预算预留 | 实际 GPU 消耗 |
|---|---|---|---:|---:|
| 2026-09-09 | 解析参照与组合误差核验 | PASS，CPU | 0 | 0 |

每个新远程作业在提交前增加记录，包含源码 commit、请求资源、墙钟时限、作业 ID 和独立目录。恢复任务时先查询已有记录，禁止因缺少输出而重复提交。
